#include "main.h"

/**
 * main - Entry
 * the function print the alphabet
 * * * in lowercase
 * * followed by a new line
 *
 * Return: Always 0.
 */
int main(void)
{
	print_alphabet();
	return (0);
}
